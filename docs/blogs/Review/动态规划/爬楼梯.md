```js
var climbStairs = function(n) {
    const dp = [1, 1];
    let start = 2;
    while (start <= n) {
        dp[start] = dp[start - 1] + dp[start - 2]
        start++;
    }
    return dp[n];
}
```